<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body{
            background-color: #dbdbdb;
        }
    </style>
</head>
<body>
<%- include('templates/navbar'); %>

<div class="container bg-white shadow rounded w-50 mt-4 text-center p-5 pb-4">
    <h1>Profile Page</h1>
    <div class="profile-image">
        <img src="https://eu.ui-avatars.com/api/?name=${<%= user.username %>}&size=250" alt="Profile Picture" />
    </div>

    <h3>User ID: <%= user.user_id %></h3>
    <h3>Name: <%= user.username %></h3>
    <h3>Email: <%= user.email %></h3>
    <h3>Role: <%= user.role %></h3>

    <div>
        <h3>Profile data info</h3>
        <h4>Created: ${convertData(<%= user.created_at %>)}</h4>
        <h4>Updated: ${convertData(<%= user.updated_at %>)} %></h4>
    </div>
</div>

<%- include('templates/footer'); %>
</body>
</html>
